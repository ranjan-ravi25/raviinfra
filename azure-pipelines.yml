trigger: none   # Pipeline sirf manually chalega

pool:
  name: 220925   # Tumhara custom agent pool

steps:
  # Step 1: Terraform install
  - task: TerraformInstaller@1
    displayName: "Install Latest Terraform"
    inputs:
      terraformVersion: 'latest'
